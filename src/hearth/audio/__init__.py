"""hearth.audio — the audio-device identity and recovery package.

Pins a direction's default device by stable identity and resolves it back to
a live index, so recovery only ever re-opens onto the same device.
"""
