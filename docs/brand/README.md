# Hearth brand assets

The Hearth mark: a flame in a fireplace, inside an amber ring, on charcoal.

| File | Use |
|---|---|
| `hearth-logo.png` | The source composite (1728×1152). Re-cut from this. |
| `hearth-mark-400.png` | Tight square mark (ring + 6% margin), 400×400 — the README header. |
| `hearth-banner.jpg` | Wide 3:2 banner (1200×800) for pages that want the full composite. |
| `hearth-avatar-512.png` | Square avatar — org / repo / app icon. |
| `hearth-social-1280x640.png` | Link-preview card (GitHub social preview, Open Graph). |
| `elements/` | Layer cutouts (RGBA): `layer-flame`, `layer-fireplace`, `layer-circular-border`, plus the composite with the ring removed. Use the flame alone at small sizes — the thin ring aliases below ~48 px. |

Integrity: `SHASUMS-256.txt` (`shasum -a 256 -c SHASUMS-256.txt`).

## Palette (sampled from the composite)

| Role | Hex |
|---|---|
| ember (ring / outer flame) | `#e0a040` · `#e08020` |
| inner flame | `#f0c050` |
| charcoal ground | `#0c1011` |

## Source and license

Generated by the Hearth maintainer with an AI image-generation tool, 2026-08-16, and adopted
as the project mark 2026-08-26. Files here carry no embedded metadata. As an AI-generated image it carries no copyright
claim; the project asserts no exclusivity over the motif. **The mark identifies the Hearth
project** — please don't use it to imply affiliation or endorsement of something else. The
code license (MIT, repo root) does not extend a trademark grant.
