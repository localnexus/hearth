# ops/compaction — session compaction, shipped with the tree

The files that write a companion's continuity note when a long talk closes, and
fold it back into the session. They ship together; the facade's compact watch
finds `compact-companion-session.sh` here when the data root has no copy of its own.

STAGED FROM THE OPERATOR TREE on 2026-09-13 (COMPACT-SHIP). The port to this
tree — the resident-door engine as the default, the lane and path defaults, the
rewritten README — is the work of the waves that follow this commit.
